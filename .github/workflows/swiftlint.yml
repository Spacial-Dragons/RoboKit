name: SwiftLint

on:
  push:        { branches: [ feat/SwiftLint ] }
  pull_request: { branches: [ feat/SwiftLint ] }

jobs:
  lint:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: GitHub Action for SwiftLint
        uses: norio-nomura/action-swiftlint@3.2.1
